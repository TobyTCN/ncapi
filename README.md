# Number Classification API (PHP)

This API classifies a given number and returns its mathematical properties along with a fun fact.

## Endpoint
**GET** `/api/classify-number?number=<number>`

## Example Request
